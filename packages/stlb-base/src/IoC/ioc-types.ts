import { GComponentList } from '../gcomponent/gcomponent-list';
import { FlexboxAdapterUtil } from '../utils/flexbox-adapter.util';

const StlbIocTypes = {
  GComponentList: GComponentList,
  FlexboxAdapterUtil: FlexboxAdapterUtil,
};

export { StlbIocTypes };
